# MPS-224 — WebGL demo (hosting mirror)

Static hosting for the **MPS-224** driving prototype, served via GitHub Pages and embedded in
Salesforce via an iframe. This repo contains only the compiled Unity WebGL build; the source
lives elsewhere. Regenerated from `Tools > MPS-224 > Build WebGL`.

Live: https://ngwarner.github.io/mps224-demo/
